"""Vibe Guard — verification layer for AI-generated code.

MVP v0.1: ingest a repo, normalize requirements into discrete feature points,
align code against those points (Route A), run security/dependency scanners
(Route C), and emit a Markdown verification report.
"""

__version__ = "0.1.0"
